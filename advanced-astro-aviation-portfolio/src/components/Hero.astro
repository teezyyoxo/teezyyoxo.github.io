
<section class="hero">

  <div class="left">
    <div class="eyebrow">SYSTEMS • AVIATION • AUTOMATION</div>

    <h1>
      BUILDING DIGITAL
      <span>COCKPITS</span>
      FOR REAL-WORLD SYSTEMS.
    </h1>

    <p>
      I build aviation-inspired interfaces, telemetry workflows,
      infrastructure tools, and polished web experiences.
    </p>

    <div class="buttons">
      <a href="#projects" class="primary">Explore Projects</a>
      <a href="#contact" class="secondary">Contact</a>
    </div>
  </div>

  <div class="right glass">
    <div class="hud-top">
      <span>HDG 274°</span>
      <span>ALT 7,500 FT</span>
      <span>SPD 148 KT</span>
    </div>

    <div class="radar">
      <div class="sweep"></div>
      <div class="ring"></div>
      <div class="ring r2"></div>
      <div class="ring r3"></div>
      <div class="blip"></div>
    </div>

    <div class="telemetry">
      <div>
        <label>Telemetry</label>
        <strong>ONLINE</strong>
      </div>

      <div>
        <label>Infrastructure</label>
        <strong>STABLE</strong>
      </div>

      <div>
        <label>Homelab</label>
        <strong>23d UPTIME</strong>
      </div>
    </div>
  </div>

</section>

<style>
.hero{
  min-height:100vh;
  display:grid;
  grid-template-columns:1.1fr 0.9fr;
  align-items:center;
  gap:70px;
  padding:0 8%;
}

.eyebrow{
  color:var(--accent);
  letter-spacing:4px;
  margin-bottom:22px;
}

.hero h1{
  font-size:5.2rem;
  line-height:0.95;
  max-width:850px;
}

.hero h1 span{
  color:var(--accent);
}

.hero p{
  margin-top:28px;
  color:var(--muted);
  max-width:650px;
  line-height:1.8;
  font-size:1.1rem;
}

.buttons{
  display:flex;
  gap:18px;
  margin-top:40px;
}

.buttons a{
  text-decoration:none;
  padding:16px 24px;
  border-radius:8px;
  font-weight:700;
}

.primary{
  background:var(--accent);
  color:#001712;
}

.secondary{
  border:1px solid var(--border);
  color:white;
}

.right{
  padding:26px;
  border-radius:8px;
}

.hud-top{
  display:flex;
  justify-content:space-between;
  color:var(--accent);
  font-family:'JetBrains Mono', monospace;
}

.radar{
  margin-top:24px;
  aspect-ratio:1/1;
  border-radius:50%;
  position:relative;
  overflow:hidden;
  border:1px solid rgba(0,224,184,0.15);
}

.sweep{
  position:absolute;
  width:50%;
  height:2px;
  background:linear-gradient(to right,var(--accent),transparent);
  top:50%;
  left:50%;
  transform-origin:left center;
  animation:spin 4s linear infinite;
}

.ring{
  position:absolute;
  inset:12%;
  border-radius:50%;
  border:1px solid rgba(0,224,184,0.12);
}

.r2{ inset:28%; }
.r3{ inset:44%; }

.blip{
  position:absolute;
  width:10px;
  height:10px;
  border-radius:50%;
  background:var(--accent);
  top:40%;
  left:58%;
  box-shadow:0 0 16px var(--accent);
}

.telemetry{
  display:grid;
  grid-template-columns:repeat(3,1fr);
  gap:14px;
  margin-top:24px;
}

.telemetry div{
  padding:18px;
  border-radius:8px;
  background:rgba(255,255,255,0.03);
}

.telemetry label{
  display:block;
  color:var(--muted);
  margin-bottom:8px;
}

.telemetry strong{
  color:var(--accent);
}

@keyframes spin{
  from{transform:rotate(0deg);}
  to{transform:rotate(360deg);}
}

@media(max-width:1000px){
  .hero{
    grid-template-columns:1fr;
    padding-top:140px;
  }

  .hero h1{
    font-size:3.6rem;
  }

  .telemetry{
    grid-template-columns:1fr;
  }
}
</style>
